import SwiftUI

/// Placeholder for the advanced Usage view (per-connection cost, charts,
/// tables, /stats-style facts). Task 10 replaces this body.
struct UsageAdvancedView: View {
    let viewModel: UsageViewModel

    var body: some View {
        EmptyView()
    }
}
